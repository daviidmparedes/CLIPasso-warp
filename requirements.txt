# Runtime dependencies. PyTorch and diffvg are installed by setup.sh, which needs
# to match them to your CUDA version and GPU architecture.
numpy
scipy
scikit-image
scikit-learn
pandas
matplotlib
opencv-python-headless
Pillow<11
imageio
imageio-ffmpeg
svgwrite
svgpathtools
cssutils
ftfy
regex
tqdm
gdown

# Optional: only needed for --use_wandb 1
# wandb
